import { Typography } from "@mui/material";

const pageStyle = {
  display: "flex",
  height: "100vh",
  justifyContent: "center",
  alignItems: "center",
  textAlign: "center",
}

const AboutUs = () => {
  return (
  <div style={pageStyle}>
    <Typography variant="h3">Brandon is GOD</Typography> 
  </div>
  );
};

export default AboutUs;
